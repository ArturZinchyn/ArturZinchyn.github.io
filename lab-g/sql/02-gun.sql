CREATE TABLE gun (
                     id INTEGER PRIMARY KEY AUTOINCREMENT,
                     name TEXT NOT NULL,
                     caliber TEXT NOT NULL,
                     magazine_capacity INTEGER NOT NULL
);